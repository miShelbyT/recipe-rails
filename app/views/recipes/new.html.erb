<h1>New Recipe</h1>

<%= form_for @recipe do |f| %>
<%= f.label :name, "Recipe Name" %>
<%= f.text_field :name %><br><br>

<%= f.label :user_id %>
<%= f.collection_select :user_id, User.all, :id, :name %><br><br>

<%= f.submit "Submit Recipes" %>
<% end %>